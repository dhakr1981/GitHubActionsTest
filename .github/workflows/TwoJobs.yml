name: Two Jobs Sample
run-name: ${{ github.event }} 
on: [workflow_dispatch]
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test this is first job"
  two:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test this second job"
